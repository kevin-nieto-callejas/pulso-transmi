PULSO_API_URL=https://pulso-transmi.72-60-245-2.sslip.io
# PULSO_API_KEY=se-habilitara-para-submissions

# Proyecto de Supabase del equipo (ver supabase/schema.sql y supabase/migrate_via_rest.py)
SUPABASE_URL=
# Lectura publica (RLS activo, solo permite SELECT): usar la anon/publishable key.
SUPABASE_KEY=
# Escrituras (collector, migraciones, GitHub Actions): requiere la service_role
# key (Supabase Dashboard > Settings > API > service_role). Nunca la expongas
# en el navegador ni en Vercel.
SUPABASE_SERVICE_ROLE_KEY=
